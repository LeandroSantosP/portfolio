# AI App (minimal)

This is a minimal, clean React + Vite starter project.

Usage:

1. Ensure you are running a Node LTS version (see package.json engines).  
2. Install dependencies: `npm install`  
3. Run the dev server: `npm run dev`  

Files added: minimal index.html, src/, vite config and package.json.
